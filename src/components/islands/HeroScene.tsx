// HeroScene — scene Three.js — nuage de particules, vanilla Three dans un composant React isole
// TODO : porter depuis CLAUDE.md §8.4. Stub d'arborescence, pas encore implemente.

export default function HeroScene(): null {
  return null;
}
